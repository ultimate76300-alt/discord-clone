PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# Railway: set CLIENT_ORIGIN to your public URL after first deploy, e.g.
# https://your-service.up.railway.app
# (Optional if you only open the app from that same URL.)
